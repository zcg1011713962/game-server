server:
  port: 18080

spring:
  application:
    name: hall-service
  datasource:
    url: jdbc:mysql://127.0.0.1:3406/game?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: yx168168
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 3000
  data:
    redis:
      host: 127.0.0.1
      port: 6479
      password:    # 没有就留空
      database: 0
      lettuce:
        pool:
          max-active: 16     # 最大连接
          max-idle: 8
          min-idle: 2
          max-wait: 3000ms
      timeout: 3000ms

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: game.hall.mapper
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
